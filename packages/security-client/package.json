{
  "name": "@tshio/security-client",
  "version": "0.0.6",
  "private": false,
  "description": "",
  "author": "tsh.io",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "docs": "typedoc --options typedoc.js",
    "build": "npm run clean && npm run compile",
    "generate-types": "tsc --declaration ./src/**/*.ts --outDir ./dist",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "npm run build",
    "integration": "mocha dist/test/**/*.spec.js --exit"
  },
  "dependencies": {
    "perron": "0.11.4",
    "qs": "6.9.4"
  },
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "typedoc": "^0.19.1",
    "typedoc-plugin-markdown": "^2.4.2"
  },
  "gitHead": "907448a1927f86abca38ec294b0e25e66c8f9716"
}
