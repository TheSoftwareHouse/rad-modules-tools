{
  "private": true,
  "npmClient": "npm",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"],
      "allowBranch": "master"
    },
    "publishConfig": {
      "directory": "dist"
    }
  },
  "packages": [
    "packages/security-client"
  ],
  "version": "independent",
  "workspaces": [
    "packages/security-client"
  ]
}
